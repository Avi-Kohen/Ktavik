# תוכנית אב — זיהוי ותרגום כתב עברי קדום

> מסמך עבודה פנימי (עברית). כל התיעוד בתוך הריפו עצמו ייכתב באנגלית — הוא נועד למעסיקים ולתורמים.
>
> גרסה 0.1 · אוגוסט 2026

---

## 0. פרופיל ואילוצים

| | |
|---|---|
| רקע | Python + בסיס ML, ללא ניסיון production |
| אנדרואיד | היכרות שטחית |
| זמן | 10–15 שעות/שבוע |
| חומרה | GPU מקומי NVIDIA + ענן לפי צורך |
| דדליין | אין קשיח |
| ריפו | ציבורי מהיום הראשון |
| יעד | פרויקט תיק עבודות שפותח דלתות למגוון משרות |

---

## 1. שלוש מסקנות אסטרטגיות שמעצבות את כל הפרויקט

### 1.1 "תרגום" הוא לא הבעיה הקשה — וחשוב מאוד שתדע להגיד את זה

כתב עברי קדום (Paleo-Hebrew) הוא **כתב**, לא **שפה**. זו אותה עברית, באלף-בית אחר. לכן ה"תרגום" מתפרק לשלושה חלקים בעלי קושי שונה לחלוטין:

| שלב | מהות | קושי | טכנולוגיה |
|---|---|---|---|
| זיהוי (OCR/HTR) | תמונה → רצף גליפים | **קשה מאוד** | Computer Vision, deep learning |
| תעתיק | פליאו → כתב אשורי (עברית מודרנית) | טריוויאלי | טבלת מיפוי 22↔22 |
| תרגום | עברית → אנגלית | פתור בעיקרו | NMT / LLM API |

**המסקנה:** 90%+ מהמאמץ ההנדסי נמצא בראייה ממוחשבת. הארכיטקטורה חייבת לשקף את זה בכנות.

בראיון עבודה, המשפט *"זיהיתי שהתרגום הוא lookup table וקריאת API, ולכן השקעתי את כל העומק בצינור הראייה"* הוא סיגנל חזק של שיקול דעת הנדסי. המשפט *"בניתי AI שמתרגם עברית עתיקה"* כשמדובר במילון — הוא דגל אדום.

### 1.2 אין דאטהסט. **מחולל הדאטה הוא לב הפרויקט.**

זו העובדה הקשה: לא קיים דאטהסט מתויג של כתב עברי קדום. הקורפוס הידוע כולו — אוסטרקונים מערד (~100 פריטים), כתובת השילוח, לוח גזר, מכתבי לכיש, חותמות, בולות, מטבעות יהוד — הוא בסדר גודל של אלפי פריטים בודדים, רובם באורך של מילים ספורות, ומפוזרים על פני פרסומים אקדמיים. אימון מפוקח קלאסי הוא בלתי אפשרי.

**אבל יש תקדים מוכח ומפורסם.** המאמר *Deep Aramaic* (PLOS One, אפריל 2024) התמודד בדיוק עם הבעיה הזו עבור ארמית עתיקה — אותה משפחת אלף-בית בת 22 אותיות, אותה תקופה. הם ייצרו 250,000 תמונות סינתטיות מגופנים + צינור השחתה פוטו-ריאליסטי, אימנו ResNet, והגיעו ל-95% דיוק על תמונות אמיתיות מכתובת האדד מהמאה ה-8 לפנה"ס.

**הפער שאתה ממלא — וזו התרומה האמיתית:**

| Deep Aramaic עשה | אתה תעשה |
|---|---|
| סיווג אותיות בודדות | זיהוי **ברמת שורה**, end-to-end |
| גליפים לבנים על רקע שחור (מגבלה שהם עצמם ציינו) | תמונות אמיתיות בשטח, עם מצע, תאורה ונזק |
| הערכה על כתובת אחת | סט הערכה אמיתי ומגוון |
| מודל במעבדה | פריסה על מכשיר + לולאת שיפור מתמשכת |

זה לא שכפול של טוטוריאל. זו תרומה שאפשר לפרסם.

### 1.3 "מודל שמשתדרג כל פעם" = Active Learning + MLOps — וזה מה שיקבל אותך לעבודה

רוב פרויקטי התיק בשוק הם notebook עם README יפה. מה שיבדל אותך:

- ניהול גרסאות דאטה (DVC) ומעקב ניסויים (MLflow/W&B)
- Model registry עם champion/challenger
- **שער הערכה ב-CI**: מודל לא נפרס אלא אם ה-CER שלו על סט האמת עובר את המודל הנוכחי
- ריטריינינג אוטומטי שמופעל מהצטברות תיקוני משתמשים
- Uncertainty sampling — המודל מבקש בעצמו את הדוגמאות שהכי יעזרו לו
- ניטור drift

הדיוק של המודל מעניין פחות מהמערכת שסביבו. זה השדר.

---

## 2. ארכיטקטורת המערכת

```
┌──────────────────────────────────────────────────────────┐
│  אפליקציית אנדרואיד (Kotlin · Compose · CameraX)         │
│  ┌────────────────────┐    ┌──────────────────────────┐  │
│  │ מסלול מהיר         │    │ מסלול מדויק              │  │
│  │ on-device (LiteRT) │    │ ענן                      │  │
│  │ גלאי + CRNN קטן    │    │ מודל מלא + LLM לתרגום    │  │
│  │ עובד אופליין       │    │ דורש רשת                 │  │
│  └────────────────────┘    └──────────────────────────┘  │
│  UI לתיקון משתמש  ──────────────┐                        │
└─────────────────────────────────┼────────────────────────┘
                                  │
┌─────────────────────────────────▼────────────────────────┐
│  שירות Inference (FastAPI · ONNX Runtime · Docker)        │
│  detection → recognition → transliteration → translation  │
└─────────────────────────────────┬────────────────────────┘
                                  │ תיקונים + דגימות אי-ודאות
┌─────────────────────────────────▼────────────────────────┐
│  לולאת Active Learning                                    │
│  תור תיוג → אימון מחדש → שער הערכה → canary → פריסה      │
└─────────────────────────────────┬────────────────────────┘
                                  │
┌─────────────────────────────────▼────────────────────────┐
│  צינור אימון (PyTorch)  ←──  מחולל דאטה סינתטי           │
│  MLflow · DVC · Model Registry                            │
└──────────────────────────────────────────────────────────┘
```

### מבנה הריפו (מונורפו)

```
old-hebrew/
├── packages/
│   ├── synth/       # מחולל דאטה סינתטי
│   ├── models/      # אימון, הערכה, ייצוא ONNX
│   ├── api/         # שירות FastAPI
│   ├── pipeline/    # תזמור active learning
│   └── android/     # אפליקציה
├── docs/
│   ├── adr/         # Architecture Decision Records
│   ├── model-card.md
│   └── dataset-card.md
├── data/            # DVC-tracked (לא ב-git)
├── infra/           # Docker, compose
└── .github/workflows/
```

### החלטות טכניות מרכזיות (כל אחת תקבל ADR)

**מודל זיהוי: CRNN + CTC לפני TrOCR.** מפתה לקפוץ ל-TrOCR, אבל: הוא ענק (לא רץ על מכשיר), דורש הרבה דאטה, ואתה לא תבין מה קורה בפנים. CRNN+CTC קטן, מתאמן מהר על ה-GPU שלך, נפרס בקלות למובייל, ותכריח אותך להבין CTC loss לעומק — שאלת ראיון קלאסית. TrOCR נשאר כ־challenger בשלב מאוחר, ואז ההשוואה ביניהם היא סיפור טוב בפני עצמה.

**פריסה: LiteRT (לשעבר TFLite) למכשיר, ONNX Runtime לשרת.** LiteRT היא הבחירה הבוגרת לאנדרואיד ב-2026 עם האצת GPU. הרציונל והחלופות ייכנסו ל-ADR.

**מסלול כפול (on-device + ענן).** לא סתם over-engineering — זה מה שמאפשר עבודה אופליין באתר ארכיאולוגי, ומדגים שיקול דעת של trade-off בין latency, דיוק וזמינות.

---

## 3. תוכנית עבודה — 6 שלבים + שלד אנכי

הערכות מבוססות 13 שעות/שבוע. סה"כ ≈ 620 שעות ≈ 10–11 חודשים.
**נקודת הצגה ראשונה: השלד האנכי (~חודש 3.5)** — צינור מלא שרץ על מכשיר אמיתי.
**נקודת הצגה שנייה, המרשימה: סוף שלב 3 (~חודש 5.5)** — זיהוי ברמת שורה עם מספרים.

---

### שלב 0 · יסודות ותשתית · 3 שבועות (~40ש)

הקמת ריפו, מונורפו, רישוי (Apache 2.0), כלי Python מודרניים (uv, ruff, mypy, pytest), pre-commit, Conventional Commits, GitHub Actions, אתר תיעוד, שני ADRs ראשונים.

**מוכיח:** אתה יודע להקים פרויקט לפי סטנדרט תעשייה, לא לזרוק קבצים בתיקייה.

---

### שלב 1 · מחולל דאטה סינתטי · 6 שבועות (~80ש) ← **תכשיט הכתר**

- איסוף גופני פליאו-עברית (Culmus Ancient, Robo-PaleoHeb, Paleo-Hebrew Abjad) — **בדיקת רישיון לכל אחד**
- קורפוס טקסט: עברית מקראית מנחלת הכלל (Westminster Leningrad Codex) → תעתיק לפליאו
- מנוע רינדור עם jitter לכל גליף (סיבוב, קנה מידה, עובי קו, סטיית קו בסיס) — לדמות כתב יד
- צינור השחתה: מצעי חרס/אבן/פפירוס, דהיית דיו, שחיקה, סדקים, מודל תאורה
- שחזוריות מלאה: seeds, קונפיגורציה, גרסאות DVC

**תוצר:** חבילת Python מותקנת + איור השוואה סינתטי↔אמיתי באיכות מאמר.
**מוכיח:** יצירתיות בפתרון מחסור בדאטה — הכישור שמייחד ML engineer טוב.

**שתי משימות רקע שמתחילות כאן** (סה"כ ~10ש) — שתיהן קיימות כדי לפרוע חוב מוקדם, לא כדי לפתח:

- **טבילת אצבע באנדרואיד (~5ש).** אפליקציה שפותחת מצלמה ומציגה תמונה. אבי לא מכיר אנדרואיד ועקומת הלמידה ארוכה; עדיף שתתחיל ברקע מוקדם ולא כקיר בשלב 5.
- **חוזה הממשק (~5ש).** מסמך OpenAPI שמגדיר מה נשלח ומה חוזר — לפני שיש מה לממש. זה מה שמאפשר לאפליקציה ולמודל להתקדם במקביל, וזו התנהגות שמצופה ממהנדס בצוות.

---

### שלב 2 · מסווג אותיות ובניית סט אמת · 4 שבועות (~55ש)

- שכפול תוצאת Deep Aramaic על פליאו-עברית (ResNet/EfficientNet)
- **בניית סט הערכה אמיתי** — קריטי, וזה החלק המפרך: תיוג ידני של כמה מאות חיתוכי אותיות מתצלומים מפורסמים (ערד, השילוח, לוח גזר, לכיש, מטבעות יהוד)
- מדדים: דיוק, F1 לכל מחלקה, confusion matrix, כיול
- MLflow/W&B מהיום הראשון
- **מדידת בסיס של הכלים הקיימים (~יום).** הרצת Tesseract ו-Google Vision על סט האמת ורישום כמה נורא הם. אין מודל מוכן לפליאו-עברית — מה שקיים הוא כתב אשורי, מערכת סימנים אחרת לגמרי, שמייצרת רעש על החומר הזה. מספר אחד ב-README שמראה את הפער בין הקיים לבין Ktavik הופך את הפרויקט מתרגיל להוכחת צורך.

**כאן תלמד בדרך הקשה מה זה sim-to-real gap.** אם הפער גדול — זה לא כישלון, זה הממצא. בונים ממנו את שלב 1 מחדש.

---

### שלב 2.5 · שלד אנכי · 2–3 שבועות (~30ש) ← **נקודת הצגה ראשונה**

פרוסה אנכית דקה שרצה מקצה לקצה: צילום אות בודדת → זיהוי → תעתיק → תרגום → תצוגה.

```
אפליקציה מינימלית → שרת מקומי → המסווג משלב 2
                               → טבלת תעתיק (22↔22)
                               → תרגום דרך ממשק חיצוני
```

שני הרכיבים האחרונים הם יומיים עבודה ביחד, ולכן מצוינים לשלד: כמעט אפס עלות, והם משלימים את הצינור.

**למה כאן ולא מוקדם יותר.** אין מודל OCR מוכן לפליאו-עברית להתחיל ממנו, אז שלד לפני שלב 2 יתחבר לפלייסהולדר — אשליה, לא הדגמה. בסוף שלב 2 יש מסווג אמיתי, קטן אבל כן, והשלד הופך למשהו שאפשר להראות.

**למה בכלל.** שני סיכונים שהשלד פורע מוקדם:

- **סיכון אינטגרציה.** הרכיב המסוכן במערכת מרובת שכבות הוא לא רכיב אלא התפרים. מודל מצוין שלא רץ על נייד הוא מודל חסר ערך, ומגלים את זה רק כשמנסים. עדיף לגלות לפני שלב 3, לא אחריו.
- **שחיקה** (סיכון 2 בפנקס). עשרה חודשים בלי משהו שרץ הם מתכון לנטישה.

**תוצר:** סרטון דמו ראשון, ו-README שכבר מראה מערכת ולא הבטחה.

---

### שלב 3 · גילוי + זיהוי ברמת שורה · 10 שבועות (~130ש) ← **החלק הקשה**

- גילוי אזורי טקסט בתצלומים אמיתיים
- CRNN + CTC לזיהוי רצפים; מדדי CER/WER
- Beam search + מודל שפה n-gram על עברית מקראית — שיפור דיוק משמעותי וחומר ראיון מצוין
- Domain adaptation: סינתטי → אמיתי

השלב הזה כבר לא נבנה בחלל: הוא מחליף את המסווג שבתוך צינור שרץ מאז שלב 2.5.

**סוף השלב = נקודת ההצגה המרשימה.** יש דמו ברמת שורה, יש מספרים, יש README עם גרפים.

---

### שלב 4 · Backend ו-MLOps · 7 שבועות (~90ש)

FastAPI אסינכרוני, סכמות Pydantic, ייצוא ONNX, model registry, Docker + compose, health checks, OpenTelemetry, rate limiting, אימות. **שער CI:** מודל לא נפרס בלי לנצח את האלוף הנוכחי.

---

### שלב 5 · אפליקציית אנדרואיד · 10 שבועות (~130ש)

Kotlin + Compose + CameraX, inference על מכשיר עם LiteRT, offline-first (Room + WorkManager), ארכיטקטורה נקייה עם MVVM ו-Hilt, שכבת overlay להצגת הזיהוי, **ו-UI לתיקון משתמש** — שמזין את הלולאה.

*כולל 2 שבועות למידה ייעודיים בתחילת השלב — אבל קלים יותר מהמתוכנן במקור, כי טבילת האצבע בשלב 1 והשלד בשלב 2.5 כבר שברו את הקרח.*

---

### שלב 6 · לולאת שיפור מתמשך ושחרור · 7 שבועות (~90ש)

קליטת תיקונים → תור תיוג → uncertainty sampling → ריטריינינג אוטומטי → שער הערכה → canary → ניטור drift.
שחרור: אתר תיעוד, model card, dataset card, סרטון דמו, העלאה ל-Hugging Face.

**אופציה:** כתיבת preprint ל-arXiv. מבדל תיק עבודות ברמה אחרת לגמרי.

---

## 4. סטנדרטים הנדסיים (לא ניתנים למשא ומתן)

- **Conventional Commits** + היסטוריה נקייה. מעסיקים קוראים את הלוג.
- **ADR לכל החלטה משמעותית.** פורמט: הקשר → החלטה → חלופות שנשקלו → השלכות.
- **בדיקות:** יחידה לכל לוגיקה, אינטגרציה ל-API, מבחן snapshot למחולל הסינתטי.
- **CI ירוק תמיד.** lint + types + tests על כל PR.
- **PR-ים גם כשאתה לבד.** feature branch → PR → self-review → merge. תרגול לעבודת צוות.
- **README שנקרא כמו מוצר**, לא כמו שיעורי בית.
- **מדדים לפני אופטימיזציה.** לעולם לא "שיפרתי" בלי מספר לפני ואחרי.

---

## 5. חוזה העבודה בינינו

**מה אני עושה:** מפרט דרישות וקריטריוני קבלה, מסביר את ה"למה" מאחורי כל בחירה, סוקר את הקוד שלך, מתיר חסימות ברמזים ולא בפתרונות, ומתשאל אותך בסגנון ראיון בסוף כל שלב.

**מה אתה עושה:** כותב את הקוד, מריץ את הפקודות, עושה את הקומיטים, מקבל את ההחלטות.

**מתי אני כן כותב קוד:** boilerplate שאין ממנו ערך לימודי, ורק אחרי שביקשת במפורש.

---

## 6. פנקס סיכונים

| סיכון | חומרה | מיטיגציה |
|---|---|---|
| פער sim-to-real גדול מדי | גבוהה | בונים סט אמת כבר בשלב 2 ולא בשלב 3; fine-tuning על סט אמיתי קטן; domain adaptation |
| אין מספיק תמונות אמיתיות להערכה | גבוהה | InscriptiFact (הרשמה חינם לחוקרים, ~68k תמונות), דוחות חפירה, אוספי מוזיאונים בגישה פתוחה, מטבעות (זמינים בשפע) |
| רישוי גופנים | בינונית | בדיקת רישיון לכל גופן, קובץ LICENSES ייעודי, העדפת גופנים פתוחים |
| עקומת למידה של אנדרואיד | בינונית | 2 שבועות למידה מתוקצבים בשלב 5 |
| זחילת היקף | גבוהה | שערי שלב עם קריטריון "בוצע" מפורש בכתב |
| שחיקה על פני 10 חודשים | גבוהה | ריפו ציבורי + סרטון דמו חודשי; שלב 3 הוא אזור הסכנה |

---

## 7. מקורות

- [Deep Aramaic: Towards a synthetic data paradigm enabling machine learning in epigraphy — PLOS One, 2024](https://journals.plos.org/plosone/article?id=10.1371%2Fjournal.pone.0299297) · [arXiv](https://arxiv.org/abs/2310.07310)
- [Computational Handwriting Analysis of Ancient Hebrew Inscriptions — A Survey](https://www.academia.edu/92834148/Computational_Handwriting_Analysis_of_Ancient_Hebrew_Inscriptions_A_Survey)
- [Forensic document examination and algorithmic handwriting analysis of Judahite biblical period inscriptions — PLOS One](https://journals.plos.org/plosone/article?id=10.1371%2Fjournal.pone.0237962)
- [Review of Computational Epigraphy — arXiv](https://arxiv.org/pdf/2406.06570)
- [InscriptiFact — West Semitic Research Project, USC](http://www.inscriptifact.com/aboutus/index.shtml)
- [The HHD Dataset — Hebrew handwritten documents](https://beratkurar.github.io/papers/icfhr2020_the_hhd_dataset.pdf)
- [Culmus Project — Ancient Semitic Scripts fonts](https://culmus.sourceforge.io/ancient/index.html)
- [Robo-PaleoHeb font — GitHub](https://github.com/edenberger/Robo-PaleoHeb)
- [Assessing advanced handwritten text recognition engines for digitizing historical documents — Springer, 2025](https://link.springer.com/article/10.1007/s42803-025-00100-0)
- [Android Neural Networks in 2026: On-Device AI with LiteRT](https://www.forasoft.com/blog/article/neural-networks-on-android-369)
