[project]
name = "ktavik-synth"
version = "0.1.0"
description = "Synthetic training-data generation for Paleo-Hebrew OCR"
readme = "README.md"
authors = [
    { name = "AviKo", email = "avicohen1021@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
